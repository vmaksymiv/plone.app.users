Introduction
============

Package for flexible user registration: allowing site manager to set fields
which appear on the join form.

2.X SERIES
==========

A z3c.form rewrite.

Make sure we keep the following functionality:

[ ] CSRF for some forms (most notably user's properties (e-mail!), also join
form)
